extends layout

block content

  script.
    // Set active Button
    document.querySelectorAll(".active")[0].classList.remove("active");
    document.querySelectorAll(".nav-link")[2].classList.add("active");

  div Hier ist die Messung.
  textarea#db(name='db' rows='4' cols='50')
  button#messung(type='button') Messung starten!

  script(src='/js/webaudio.js')